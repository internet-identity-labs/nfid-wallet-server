type Conf = record {
    ledger_canister_id : principal
};

type Group = record {
    participants : vec nat64;
    name : text;
    accounts : vec nat64;
    group_id : nat64;
};

service : (Conf) -> {
    register_group : (text) -> (Group);
    sub : (text, nat64, nat8) -> (text) query;
}