type Conf = record {
    im_canister : opt text
};

type ICRC1 = record {
    ledger : text;
    index : opt text
};

service : (Conf) -> {
    get_canisters_by_root : (text) -> (vec ICRC1) query;
    store_icrc1_canister : (text, opt text) -> ();
}