(
  record {
    data = opt record {
      name = "TEST_USER";
      personas = vec {};
      principal_id = "%s";
      phone_number = "123456";
      devices = vec {
        record {
          model = "IProboval";
          pub_key_hash = "PUB_KEY_HASH";
          make = "CHINA";
          browser = "CHROME";
          last_used = "123";
        };
      };
    };
    error = null;
    status_code = 200 : nat16;
  },
)
